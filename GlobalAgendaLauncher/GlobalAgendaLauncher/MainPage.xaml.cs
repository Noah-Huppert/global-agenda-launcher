namespace GlobalAgendaLauncher;

public partial class MainPage : ContentPage
{

	public MainPage()
	{
		InitializeComponent();
	}

	private void OnLaunchClicked(object sender, EventArgs e)
	{
		/*
		count++;

		if (count == 1)
			LaunchBtn.Text = $"Clicked {count} time";
		else
			LaunchBtn.Text = $"Clicked {count} times";

		SemanticScreenReader.Announce(LaunchBtn.Text);
		*/
	}
}

